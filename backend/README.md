# KFZ Parts Backend

NestJS + TypeORM API for the KFZ spare parts inventory system.

## Scripts

- `npm install`
- `npm run start:dev`
- `npm run build`

Configure environment variables via `.env` (see `.env.example`).


